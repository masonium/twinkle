#include "geometry.h"

void Geometry::texture_coord(SubGeo UNUSED(subgeo), const Vec3& UNUSED(pos), const Vec3& UNUSED(normal),
                             scalar& UNUSED(u), scalar& UNUSED(v)) const
{
}
